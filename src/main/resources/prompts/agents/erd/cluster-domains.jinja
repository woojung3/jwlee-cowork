The following is a list of database tables extracted from a backend project.
Your task is to cluster these tables into logical, cohesive domains (e.g., 'User Management', 'Order System', 'Billing').

RULES:
1. Divide ALL tables into domains. No table should be left out.
2. A domain should typically contain **10 to 15 tables**.
3. If a logical domain naturally exceeds **20 tables**, you MUST limit it to the most core 20 tables and provide a brief `codeSmellWarning` (in Korean) about the domain being too bloated/coupled.
4. Output the clustering results as a structured list of domains.

TABLE LIST:
{{ tables }}